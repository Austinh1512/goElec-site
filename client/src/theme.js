import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#00A67C"
        },
        secondary: {
            main: "#A6002A"
        }
    }
})

export default theme;